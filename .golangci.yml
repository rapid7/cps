linters:
  disable-all: true
  enable:
    - govet
    - vet
    - staticcheck
    - golint
    - errcheck
    - ineffassign
    - misspell
  enable-all: false

issues:
  exclude-use-default: false

run:
  concurrency: 2
  deadline: 20m
